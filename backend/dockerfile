FROM node:20-slim

RUN adduser --system --uid 1001 --shell /usr/sbin/nologin --no-create-home nodeuser

WORKDIR /app

COPY package*.json ./
RUN npm install --omit=dev && npm cache clean --force

COPY --chown=nodeuser:nodeuser . .

EXPOSE 8080

USER nodeuser

CMD ["npm", "start"]
